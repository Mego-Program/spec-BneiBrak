import { createTheme } from '@mui/material/styles';


const theme = createTheme({
  palette: {
    primary: {
      main: '#F6C927',
    },
    secondary: {
      main: '#121231',
    },
    background: {
      default: '#21213E',
    },
  },
  typography: {
    fontFamily: 'Poppins',
  },
});

export default theme;
